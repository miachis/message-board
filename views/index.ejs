<html>
  <head></head>
  <body>
    <% messages.forEach((message) => { %>
    <li>
      <p><%= message.text %></p>
      <p><%= message.user %></p>
      <p><%= message.added %></p>
    </li>
    <% }); %>
    <a href="/new">New</a>
  </body>
</html>
